version: "3.1"

services:
  php-fpm:
    build:
      context: docker/php-fpm
    container_name: example-webserver
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - WEB_DOCUMENT_ROOT=/app/www
    ports:
      - "80:80"
